const Lightpick = require('lightpick')
var picker = new Lightpick({
    field: document.getElementById('first-picker'),
    secondField: document.getElementById('second-picker'),
    singleDate: false,
});